# Dashboard Contract

Stable output fields: model_run_id, scenario_id, sector, geography, year, metric, unit, value,
formula_version, adjustment_version and approval_status. React consumes the API contract and does not call Spark.
